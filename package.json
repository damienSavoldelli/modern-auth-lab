{
  "name": "modern-auth-lab",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Frontend tooling for the Modern Auth Lab educational authentication project.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@vitest/coverage-v8": "^4.0.0",
    "eslint": "^9.0.0",
    "globals": "^16.0.0",
    "prettier": "^3.0.0",
    "vite": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
